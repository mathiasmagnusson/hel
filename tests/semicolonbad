let a =
2 let b = 5

let foo = bar; // without this semicolon, this would be interpreted as a struct of type bar
{
    print_shit("such a necessary code block")
}

if a > 2
    b = 2
else
    for i in range(20) b = foo(b)

let list = if b <= 6 and 2 < a
then // without this `then`, we would be indexing into a
    [1, foo(a)]
else
    [foo(a**b), foo(3 & a)]

if (a == 2) {
    jes ples
} else {
    no pleas
}

if do_things then {
    do_stuff();
}